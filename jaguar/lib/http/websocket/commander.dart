part of 'websocket.dart';

/* TODO
RouteHandler wsCommander<T>(
    WsOnConnect onConnect,
    {Encoding encoding}) {
  return (Context ctx) async {
    final WebSocket ws = await ctx.req.upgradeToWebSocket;
    ctx.response = SkipResponse();
    // TODO
  };
}
*/
